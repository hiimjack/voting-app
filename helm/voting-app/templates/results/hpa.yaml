{{- if .Values.results.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "voting-app.fullname" . }}-results
  labels:
    {{- include "voting-app.results.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "voting-app.fullname" . }}
  minReplicas: {{ .Values.results.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.results.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.results.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.results.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.results.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.results.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}

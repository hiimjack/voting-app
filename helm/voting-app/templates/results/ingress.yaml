{{- if .Values.results.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "voting-app.fullname" . }}
  labels:
    {{- include "voting-app.results.labels" . | nindent 4 }}
  {{- with .Values.results.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.results.ingress.className }}
  ingressClassName: {{ . }}
  {{- end }}
  {{- if .Values.results.ingress.tls }}
  tls:
    {{- range .Values.results.ingress.tls }}
    - hosts:
        {{- range .hosts }}
        - {{ . | quote }}
        {{- end }}
      secretName: {{ .secretName }}
    {{- end }}
  {{- end }}
  rules:
    {{- range .Values.results.ingress.hosts }}
    - host: {{ .host | quote }}
      http:
        paths:
          {{- range .paths }}
          - path: {{ .path }}
            {{- with .pathType }}
            pathType: {{ . }}
            {{- end }}
            backend:
              service:
                name: {{ include "voting-app.fullname" $ }}
                port:
                  number: {{ $.Values.results.service.port }}
          {{- end }}
    {{- end }}
{{- end }}
